# 打非治违任务闭环业务说明（2026-07-15）

## 1. 文档用途

本文档用于统一打非治违模块的业务口径，供后续产品设计、原型设计、开发和其他对话直接引用。

本次业务由原来的四类处置逻辑调整为一套统一任务闭环，并按县级、市级、省级三个任务层级运行。首页负责说明总体情况，下钻页面承载任务明细和闭环过程。

## 2. 核心业务目标

系统持续汇聚煤矿相关算法线索，分析每座煤矿在不同专题整治场景下的风险，并通过“智能触发为主、人工发起为辅”的方式生成核查任务。任务自动下发至对应煤矿的驻矿员组，由当班驻矿员现场核实，经相应层级复核后形成闭环。

闭环过程需要支持：

- 线索持续新增和专题风险评分动态变化。
- 按评分阈值智能触发县级、市级或省级任务。
- 未达到自动触发阈值时，由有权限的单位人工发起任务。
- AI 基于线索生成现场检查清单，人工可追加、删改和合并检查内容。
- 多个专题整治场景的检查内容合并到同一现场任务中处理。
- 任务自动下发至煤矿驻矿员组，由当班驻矿员承接执行。
- 上级复核、打回重查或由上级人员重新开展现场核实。
- 闭环前风险评分突破更高阈值时，任务动态升级并调整复核流程。

## 3. 业务对象与基础口径

### 3.1 算法线索

系统持续生成并汇聚五类算法线索：瓦斯、风、人、煤、电。

线索是专题分析和现场核查内容的基础。单条线索不直接等同于专题风险，必须经过专题分析和现场核实。

### 3.2 专题整治场景

当前包含三个专题整治场景：

1. 隐蔽工作面专项整治。
2. 监控系统造假专项整治。
3. 隐瞒入井人数专项整治。

同一条线索可以参与一个或多个专题的分析；同一座煤矿也可以同时在多个专题下形成风险评分。

### 3.3 专题风险评分

评分对象为“煤矿 + 专题整治场景”。系统基于该煤矿相关线索进行 AI 分析，形成专题风险评分。

- 分数会随线索新增、线索核查结果和分析结果变化而动态更新。
- 不同分数区间对应县级、市级、省级任务触发阈值。
- 评分达到相应阈值后，系统智能触发对应层级任务。
- 某一阈值区间可以不自动发起任务，但有权限的单位仍可人工发起。
- 智能触发是主要业务路径，人工发起是补充路径。

### 3.4 核查任务

县级、市级、省级任务均属于同一种专题核查任务，主要区别是任务层级、发起责任范围、复核链路和监管强度，不是三套独立业务流程。

任务数量不能与高风险矿井数量直接对应。中风险和中高风险也会自动生成任务；同一煤矿在多个专题下需要核查时可以合并为一个现场执行任务；未闭环任务在风险等级上升时沿原链路升级，不重复生成独立任务。因此，首页应分别展示风险等级构成和当期任务情况，不使用“高风险矿井任务覆盖率”表达二者关系。

任务至少记录：任务编号、层级、地区、煤矿、触发方式、触发时评分和阈值、关联专题、关联线索、检查清单、驻矿员接收范围、实际执行人、现场证据、核查结论、复核记录、升级记录和最终闭环结论。

## 4. 两种任务发起方式

### 4.1 智能触发

智能触发是主流发起方式。

1. 系统持续计算煤矿在各专题下的风险评分。
2. 低风险不自动发起任务；中风险、中高风险和高风险达到对应等级阈值。
3. 系统按“中风险—县级、中高风险—市级、高风险—省级”的映射确定任务层级。
4. AI 汇总相关线索，生成现场核查清单。
5. 系统生成任务并进入“待办”状态。

“智能触发”仅表示任务由评分规则自动生成，不表示 AI 自动完成现场核查或复核。

### 4.2 人工发起

人工发起用于未达到智能触发阈值，但监管人员认为仍有必要现场核实的情况。

1. 有权限的单位选择煤矿和专题。
2. 选择需要纳入任务的线索，或补充人工发现的问题。
3. 发起单位按自身权限和业务判断确定任务层级。
4. AI 根据选定线索生成检查清单，发起人员可修改。
5. 任务生成后进入“待办”状态。

人工发起不改变后续驻矿员下发、现场执行和多级复核逻辑。

## 5. 检查内容生成、追加与合并

### 5.1 AI 生成检查清单

AI 以任务关联线索为依据生成现场检查内容。每个检查项应能够追溯到对应线索和专题分析依据，不能只输出无法解释的通用检查要求。

检查项建议包含核查对象和位置、核查事项、对应线索、需要采集的证据、判定要求、关联专题。

### 5.2 人工修改

任务发起后，具备权限的人员可以追加、修改或合并检查内容。每次调整需要保留修改人、修改时间、修改原因和版本记录。

### 5.3 多专题合并处理

同一煤矿在多个专题下需要核查时，可以合并为一个现场执行任务，避免驻矿员重复到场和重复取证。

合并后必须保留：

- 各专题的独立风险评分和触发依据。
- 各专题对应的线索与检查项。
- 各专题独立的核查结论。
- 合并前任务或待生成任务之间的来源关系。

界面和文档中不使用含义模糊的“任务关联”作为独立业务名称。需要表达时，分别使用“合并任务”“关联专题”或“关联线索”。

## 6. 自动下发与驻矿员执行

四川一座煤矿通常配置多名驻矿员轮流当班，因此任务不由监管人员手工指定给某一个驻矿员。

1. 任务生成后，系统根据目标煤矿自动下发给该矿的驻矿员组。
2. 驻矿员组内符合权限的人员都可以收到任务提醒。
3. 当前当班驻矿员选择承接任务后，系统记录实际执行人。
4. 任务由“待办”进入“执行中”。
5. 执行人按检查清单开展现场核查，采集照片、视频、文档、设备记录或其他证据。
6. 执行人完成全部必填检查项并提交报告后，任务进入“复核中”。

需要避免把“人工发起任务”误解为“人工安排驻矿员”。前者决定是否生成任务，后者在当前规则下由系统自动下发、当班人员承接。

## 7. 现场核查结论

现场核查需要同时回答两个问题：线索所述问题是否存在，以及该问题是否确实指向当前专题风险。最终结论分为三类：

| 结论 | 问题是否存在 | 是否指向专题风险 | 业务含义 |
|---|---|---|---|
| 问题存在，但不构成该专题风险 | 是 | 否 | 线索发现的问题真实存在，但原专题指向不准确；保留问题事实，不按该专题风险确认 |
| 问题不存在 | 否 | 否 | 现场证据不能证明线索所述问题存在，记录为线索不成立 |
| 问题存在，且构成该专题风险 | 是 | 是 | 线索成立，并确认具有专题指向，需要按专题风险进入后续处理和复核 |

当一个任务合并多个专题时，应按专题分别给出结论，不能用一个总结果覆盖全部专题。

## 8. 复核与闭环

驻矿员提交报告后，任务进入与当前任务层级对应的多级复核流程。具体复核节点和参与单位由后续权限及流程配置确定，但业务能力必须包含：

- 复核通过：确认核查材料和结论，完成当前复核节点。
- 打回重新核实：说明打回原因和需要补充的检查内容，由驻矿员重新现场核查或补充证据。
- 上级重新现场核实：上级人员可承接现场复核，自行到矿重新核实情况并提交复核结果。
- 继续流转：未到最终节点时，进入下一复核节点。
- 闭环：所有规定复核节点通过后，任务进入“已闭环”。

闭环不是简单的“报告已提交”，而是任务执行、证据、专题结论和规定复核流程均已完成。

## 9. 闭环前动态升级

专题风险评分在任务执行和复核期间仍会持续变化。

### 9.1 升级条件

任务尚未闭环，且对应煤矿的专题风险评分突破更高层级阈值时，系统触发任务升级：县级可升级为市级，市级可升级为省级；省级已经是最高层级，不再升级。

### 9.2 升级后的处理

任务升级后不重新生成一条互不相关的新任务，而是在原任务链路上保留升级前后的完整记录，并执行以下调整：

- 更新当前任务层级。
- 调整后续复核节点和责任范围。
- 保留此前已完成的现场检查、证据和复核记录。
- 重新分析新增线索，生成或追加检查项。
- 判断新增内容是否需要驻矿员重新现场核查。

新增线索或新增检查项出现后，可以要求驻矿员重新核查，也可以在证据充分时不要求重新核查。

- 要求重查：任务退回执行环节，明确新增检查内容，尤其优先核查新出现的线索。
- 不要求重查：保留原执行结果继续复核，并记录决定、理由和操作人。

不得把“新增检查待确认”设计成独立业务状态。其本质是升级或新增线索后，对“是否追加检查项、是否要求重新核查”的处理决策。

## 10. 任务状态模型

主状态统一为：

1. 待办：任务已生成并下发至驻矿员组，尚未由当班人员承接。
2. 执行中：当班驻矿员已承接，正在现场核查或补充材料。
3. 复核中：执行报告已提交，正在按当前层级进行多级复核。
4. 已闭环：规定复核节点全部通过，任务完成。

打回重查、上级现场重核和任务升级属于过程事件，不单独替代四个主状态：打回重查后回到“执行中”；上级现场重核仍属于“复核中”；升级后根据是否要求重新核查进入“执行中”或继续“复核中”。

```mermaid
flowchart LR
    A[线索持续生成] --> B[按煤矿和专题动态评分]
    B --> C{是否达到触发阈值}
    C -->|达到| D[智能触发对应层级任务]
    C -->|未达到但有核查必要| E[人工发起任务]
    D --> F[AI生成检查清单]
    E --> F
    F --> G[待办：自动下发驻矿员组]
    G --> H[执行中：当班驻矿员承接并核查]
    H --> I[提交现场报告]
    I --> J[复核中：按任务层级多级复核]
    J -->|通过全部节点| K[已闭环]
    J -->|打回重查| H
    J -->|上级现场重核| J
    B -->|闭环前突破更高阈值| L[任务升级并调整流程]
    L -->|需要新增现场核查| H
    L -->|无需重新核查| J
```

## 11. 首页与下钻页面的职责边界

### 11.1 首页模块

首页打非治违区域主要用于说明总体情况，不作为任务池工作台。展示五类算法线索数量、三个专题的任务及状态情况、县级/市级/省级任务总数及待办/执行中/复核中/已闭环数量。

人工发起任务可以纳入对应专题统计；无法归入现有专题的人工任务应单独统计，不能强行归类。

首页不重点展示发起方式、是否升级或“升级任务”数量、驻矿员分派操作、进入任务管理按钮。

### 11.2 下钻总览页

下钻后的首页仍以总体说明为主，不应默认直接进入省级任务池工作台。顶部采用页面标题、地区筛选、时间筛选和退出按钮。地区直接选择，不设置“省级 / 市级 / 县级”三级切换按钮；页面主体展示专题构成、任务层级构成、状态分布、区域分布和变化趋势。

## 12. 已确认规则

- 统一为一套任务闭环，按县、市、省三个层级运行。
- 智能触发和人工发起两条规则同时成立，智能触发是主路径。
- 低风险不自动发起任务；中风险触发县级任务，中高风险触发市级任务，高风险触发省级任务。
- 智能触发时由评分区间确定任务层级。
- 人工发起时由发起单位结合权限确定任务层级。
- 检查内容基于线索由 AI 生成，支持追加、修改和多专题合并。
- 任务生成后进入待办，并自动下发至目标煤矿驻矿员组。
- 由当班驻矿员承接执行，不由监管人员手工指定单个驻矿员。
- 核查结论分为三类，并需要区分“问题存在”与“专题指向成立”。
- 任务闭环前评分突破更高阈值时动态升级，省级不再升级。
- 升级后可以要求重新核查，也可以不要求，但需要形成处理记录。

## 13. 待配置或待确认项

- 低风险、中风险、中高风险、高风险的具体评分边界，以及县级、市级、省级任务的具体触发分值和计算规则。
- 各层级多级复核的具体节点、顺序、角色和时限。
- 人工发起任务的单位权限范围和跨区域权限。
- 驻矿员排班数据来源、无人当班或多人同时承接时的处理规则。
- 任务超期、催办、转交和异常终止规则。
- 评分下降后是否允许降级；当前未形成规则，不应默认自动降级。
- 已闭环后出现新线索时，是生成新任务还是允许重启原任务。
- 合并任务的限制条件和拆分规则。

## 14. 关键验收场景

1. 某煤矿专题分数未达到自动触发阈值时，不自动生成任务；有权限单位可以人工发起。
2. 专题分数达到县级阈值时，系统自动生成县级任务、AI 检查清单并进入待办。
3. 任务自动下发给该矿驻矿员组，当班驻矿员承接后进入执行中。
4. 一个煤矿同时命中两个专题时，可以合并现场执行，但两个专题保留独立结论。
5. 驻矿员提交“问题存在但不构成该专题风险”时，系统同时记录问题事实成立和专题指向不成立。
6. 复核人员打回后，任务回到执行中并携带明确的补查要求。
7. 市级任务闭环前分数达到省级阈值时，任务升级为省级，原证据保留，后续复核链路调整。
8. 任务已是省级时，即使分数继续上升，也不出现“升级任务”入口或数量。
9. 新增线索产生新检查项时，可选择要求驻矿员重查或继续复核，两种选择均保留依据和操作记录。
10. 所有规定复核节点通过后，任务才进入已闭环。